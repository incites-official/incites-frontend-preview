# INCITES 프론트엔드 디자인 기준

## 기준 자료

- `design/logo.svg`: 상단 좌측 브랜드 로고
- `design/Side Bar.svg`: 상단바와 좌측 내비게이션 프레임
- `design/Color System.svg`: 색상 토큰
- `design/Typography.svg`: Pretendard 타이포그래피
- `design/공용 컴포넌트.svg`: 버튼, 입력, 배지, 모달 등 공통 요소
- `design/운영 대시보드.png`: 운영 대시보드의 기본 밀도와 카드 배치
- `design/관리자 관리.svg`: 관리자 목록과 기능별 권한 매트릭스
- `일반관리자(GA).svg`, `총괄관리자(SA).svg`: 역할별 정보 구조와 업무 화면
- `학생(ST).svg`, `학부모(PA).svg`: 학생·학부모 모바일 홈, 성장, 리포트, 자녀, MY 화면

외부 디자인 원본은 구현 판단을 위한 참고 자료이며 런타임 에셋으로 직접 사용하지 않는다.

## 디자인 토큰

- 기본 글꼴: `font/Pretendard-1.3.9/web/variable/woff2/PretendardVariable.woff2`
- 로딩 방식: 루트 레이아웃의 `next/font/local`을 통해 `Pretendard Variable` 45~920 가변 굵기를 자체 호스팅
- 본문 색상: `#1e293b`
- 보조 색상: `#64748b`
- 캔버스: `#f1f5f9`
- 주요 색상: `#257ddb`
- 브랜드 그라데이션: `#2596f0` → `#4cd3c8`
- 성공: `#22c55e`
- 경고: `#f5a524`
- 오류: `#ef4444`
- 카드: 흰색 배경, `#cbd5e1` 테두리, 18~20px 모서리

구현 토큰은 `app/globals.css`의 `:root`를 단일 기준으로 사용한다.

### 타이포그래피

`design/Typography.svg`를 기준으로 다음 크기·굵기·자간을 사용한다. 화면에서는 400, 500, 700을 우선 사용하며 숫자 지표와 SVG 차트 레이블도 같은 로컬 폰트를 상속한다.

| 스타일 | 굵기 | 크기 | 자간 |
| --- | ---: | ---: | ---: |
| Display 1 | 700 | 48px | -2% |
| Display 2 | 500 | 40px | -2% |
| Heading 1 | 700 | 36px | -2% |
| Heading 2 | 700 | 32px | -2% |
| Heading 3 | 700 | 24px | -2% |
| Title | 700 / 500 | 20px | -2% |
| Subtitle 1 | 700 / 500 / 400 | 18px | -2% |
| Subtitle 2 | 700 / 500 / 400 | 16px | -1% |
| Body 1 / Label 1 | 700 / 500 / 400 | 14px | -1% |
| Body 2 | 500 / 400 | 12px | -1% |
| Label 2 / Caption 1 | 700 / 500 / 400 | 12px | -1% |

## 역할별 화면

### 총괄관리자 (SA)

- 운영 대시보드: 전체 학생, 활성 관리자, 관찰 기록, 리포트 공개율
- 관찰 기록 추이와 INCITES 역량 분포
- 최근 활동 로그와 처리 대기 항목
- 관리자 계정 및 기능별 권한 관리
- 사용자 관리: 학생 목록, 보호자 연결 승인, 촬영 동의 관리
- 기록 관리: 성장 리포트 공개·회수, 미열람 추적, 전체 관찰 기록 감사
- 설정: Level 기준, 리포트 공개 정책, 세션 및 기록 정책

| 메뉴 | 경로 | 주요 상태와 동작 |
| --- | --- | --- |
| 대시보드 | `/dashboard` | 운영 지표, 최초 API 상태 확인과 수동 새로고침, 최근 활동 |
| 관리자 관리 | `/users` | 계정 목록, 등록·수정, 권한 매트릭스 |
| 사용자 관리 · 학부모 목록 | `/members/guardians` | 보호자 연결 승인·거절 |
| 사용자 관리 · 학생 목록 | `/members/students` | 학생 원장, 연결 상태, 촬영 동의 |
| 기록 관리 · 관찰 기록 | `/records/audit` | 전체 관찰 기록 Read-Only 감사 |
| 기록 관리 · 리포트 관리 | `/records/reports` | 공개 관리, 미열람 추적 |
| 설정 | `/settings` | 평가·리포트·세션 정책 |

### 일반관리자 (GA)

- 선생님 대시보드: 오늘 수업 학급과 담당 학생
- 관찰 필요, 작성 중, 리포트 검토 상태
- 담당 학생별 누적 관찰과 최근 기록일
- 학생 성장 관리: 기본정보, 관찰 현황, 회차별 기록, 성장 추이, 리포트
- 관찰 기록 작성: 학생, 수업, 핵심영역, Level, 태그, 코멘트, 사진, 검토의 8단계 흐름
- 임시저장함과 AI 성장 리포트 검토·승인

| 메뉴 | 경로 | 주요 상태와 동작 |
| --- | --- | --- |
| 대시보드 | `/dashboard` | 담당 학생과 오늘 수업 현황 |
| 담당 학생 목록 | `/students` | 학생별 탭 기반 성장 정보 |
| 관찰 기록 · 관찰 기록 작성 | `/observations/write` | 8단계 작성·검증·저장 완료 |
| 관찰 기록 · 임시저장함 | `/observations/drafts` | 이어쓰기와 삭제 확인 |
| 기록 관리 · 리포트 검토 | `/reports` | 보호자/학생 미리보기, AI 코멘트 편집, 공개 승인 |

프론트의 기존 역할 값은 백엔드 계약이 확정되기 전까지 `ADMIN`을 SA, `OPERATOR`를 GA로 표시한다. 이 대응 관계는 표시용이며 최종 권한 모델을 확정하는 계약이 아니다.

### 학생 (ST)

| 메뉴 | 경로 | 주요 상태와 동작 |
| --- | --- | --- |
| 홈 | `/student` | 월간 성장 요약과 7대 역량 배지 |
| 성장 | `/student/growth` | 역량 레이더와 영역별 성장 레벨 |
| 리포트 | `/student/report` | 월간 총평, 역량 차트, 공유·PDF 동작 안내 |
| MY | `/student/my` | 학생 계정 정보, 알림 설정, 로그아웃 |

### 학부모 (PA)

| 메뉴 | 경로 | 주요 상태와 동작 |
| --- | --- | --- |
| 홈 | `/parent` | 자녀 전환, 성장 요약, 레이더, 최근 관찰 기록 |
| 자녀 | `/parent/children` | 자녀 인증코드 연결과 촬영 동의 상태 변경 |
| 성장 | `/parent/growth` | 선택 자녀의 역량 레이더와 성장 레벨 |
| 리포트 | `/parent/report` | 월간 총평, 역량 차트, 공유·PDF 동작 안내 |
| MY | `/parent/my` | 학부모 계정 정보, 알림 설정, 로그아웃 |

## UI 미리보기와 데이터 경계

- 상단바의 학생 컨텍스트 칩을 열어 SA와 GA 화면을 즉시 전환하며, ST와 PA 링크로 모바일 화면에 진입한다. 미리보기 제어가 대시보드 본문 배치를 밀어내지 않도록 프레임 안에 배치한다.
- 선택한 미리보기 역할은 `sessionStorage`에 보관하여 메뉴 이동과 새로고침 후에도 유지한다.
- 실제 로그인 역할은 `ADMIN`을 SA, `OPERATOR`를 GA로 대응하며, 화면 미리보기 전환이 실제 인증 권한을 높이지는 않는다.
- 관리자 관리 화면은 기본적으로 `더미 데이터`를 사용한다.
- 더미 관리자 등록, 수정, 비밀번호 변경, 사용 중지는 메모리에서만 동작하며 새로고침하면 초기화된다.
- `실제 API`를 선택하면 기존 사용자 관리 API를 사용한다.
- 더미 데이터는 `lib/mocks/admin-ui.ts`에만 둔다. 실제 API 응답에 더미 값을 섞거나 API 오류를 더미 값으로 감추지 않는다.
- 역할별 업무 화면의 더미 데이터는 `lib/mocks/workspace-ui.ts`에서만 관리한다. 화면 안에서 임의의 업무 데이터를 만들지 않는다.
- 학생·학부모 화면의 더미 데이터는 `lib/mocks/mobile-ui.ts`에서 관리한다.
- 보호자 승인, 촬영 동의, 리포트 공개, 관찰 기록 저장 같은 업무 동작은 현재 UI 확인용 로컬 상태이며 백엔드 계약 확정 후 실제 API로 교체한다.
- 대시보드는 진입 시 `/api/dashboard/summary`를 한 번 호출하며 이후에는 사용자가 새로고침 버튼을 눌렀을 때만 다시 호출한다.

## 반응형 기준

- `design/운영 대시보드.png`의 1920×1107 기준 좌표는 콘텐츠 시작 `x=360, y=120`, 지표 `132px`, 주요 차트 `396px`, 하단 패널 `265px`, 섹션 간격 `24px`로 맞춘다.
- 100% 브라우저 배율의 1440×1000 화면에서는 높이 기반 밀도 규격으로 전환하여 핵심 대시보드가 한 화면에 들어오도록 한다.
- 1536×864 이하 높이에서는 상단바, 지표, 차트와 하단 패널 높이를 한 단계 더 줄이되 글꼴을 브라우저 확대/축소로 축소하지 않는다.
- 1100px 이하에서는 대시보드 복합 그리드와 관리자 화면을 순차 배치한다.
- 820px 이하에서는 좌측 내비게이션을 오프캔버스 메뉴로 전환한다.
- 600px 이하에서는 미리보기 제어, 지표, 학생 카드, 처리 대기를 한 열로 배치한다.
- 작은 화면에서 콘텐츠를 억지로 축소하지 않고 세로 스크롤을 허용한다. 가로 스크롤은 만들지 않는다.
- 학생·학부모 화면은 데스크톱에서도 최대 390px 폭을 유지하고 가운데 배치하며, 390px 이하에서는 화면 폭 전체를 사용한다.
- 학생·학부모 화면은 상단 브랜드 영역과 하단 역할별 내비게이션을 고정하고 본문만 세로 스크롤한다.

## 공통 컴포넌트 원칙

- 버튼, 입력, 선택, 배지, 카드, 모달, 인라인 메시지는 `components/soft`의 공통 요소를 우선 사용한다.
- 브랜드 로고는 `BrandLogo`, 아이콘은 `Icon`, 프레임은 `AppShell`, 사이드바 항목은 `SidebarMenuGroup`을 사용한다.
- 업무 화면에서 반복되는 탭, 검색 입력, 아바타, 상태 배지, 빈 상태는 `components/workspace/WorkspaceUi.tsx`의 공통 요소를 사용한다.
- 학생·학부모 공통 헤더와 하단 메뉴는 `MobileRoleShell`, 성장 레이더·역량 카드·리포트는 `MobileScreens`의 공통 컴포넌트를 사용한다.
- 패널 제목, 관리자 목록, 권한표와 역할별 복합 업무 영역은 기능 단위 컴포넌트로 유지한다.
- 화면명, 변수명, 문구에는 과거 참고 시스템의 명칭을 사용하지 않는다.
